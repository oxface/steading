apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: seneschal
spec:
  selector:
    app.kubernetes.io/component: api
    deployment-slot: blue
  ports:
    - name: http
      port: 8000
      targetPort: http
